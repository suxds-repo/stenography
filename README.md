# Стеганография в тексте (Text Steganography)

Программа на Python для **скрытия и извлечения текстовых сообщений в текстовых файлах** с использованием простого метода стеганографии: добавления пробелов в конце строк.

---

## 🔹 Функционал

1. **Скрытие текста (Hide Text)**
   - Секретный текст кодируется в бинарный вид.
   - Каждый бит скрывается через **пробел в конце строки** файла-контейнера.
   - Результат сохраняется как новый файл — **стегофайл**.

2. **Извлечение текста (Extract Text)**
   - Читает стегофайл.
   - Определяет биты по наличию пробелов в конце строк.
   - Восстанавливает оригинальное сообщение.

---

## 🔹 Требования

- Python 3.7+
- Модуль `tkinter` (обычно установлен по умолчанию)
- Работает с текстовыми файлами (`.txt`)

---

## 🔹 Установка

1. Склонируйте репозиторий или скачайте скрипт:
```bash
git clone https://github.com/suxds-repo/stenography.git
```


## 🔹 Использование

После запуска скрипт спросит режим работы:
```bash
Выберите режим (hide/extract):
```

- hide — скрыть сообщение в файле.

- extract — извлечь сообщение из стегофайла.

**Скрытие текста**

- Выберите файл-контейнер (обычный текстовый файл).

- Выберите файл с секретным сообщением.

- Укажите путь для сохранения стегофайла.

- Нажмите "Скрыть текст".

**Извлечение текста**

- Выберите стегофайл.

- Укажите путь для сохранения извлечённого текста.

- Нажмите "Извлечь текст".
